# MIT-Missing-Semester
## shell
- 空格分隔参数
- 环境变量 搜索程序位置 启动shell时设置
- 搜索路径 关键变量——路径变量 查看echo $PATH 路径列表用冒号分隔
- which 输出运行的程序的路径
- 绝对路径
- 相对路径
- pwd输出当前路径
- cd 改变目录
- .当前目录
- ..父目录
- 当前目录下不需要程序地址
- ls列出当前目录的文件
- ls可以加目录
- ～主目录
- 代替相对路径的开头？
- cd -回到上一个目录
- 不带值是flag，带值是option
- ls -l详细信息
- d 目录 前三个字符文件所有者的权限
- 第二组三个字符是拥有文件的组的权限
- 最后三个字母是其他人的权限
- r读取w写入x执行
- 文件和目录的权限含义不同
- 文件是字面意思
- 目录r 允许查看目录中的文件ls
- w在该目录中重命名，创建，删除文件
- x搜索，是否允许进入目录cd（该文件及其所有父目录）
- mv 移动或改名
- mv 原名 改的名字
- cp 复制 
- cp 要复制的文件路径 目标路径
- rm 删除文件 需要提供路径
- 删除不会递归进行，不能用直接rm删除文件夹
- rm -r删除路径下所有内容
- rmdir删除一个目录，只能删除空目录
- mkdir创建新目录
- man +其他程序名 手册页
- ctrl+l清屏
- 流
- <重定向为输入
- >重定向为输出
- cat 打印文件内容
- cat 复制文件内容
- (echo不能接受文件输入？）
- >>追加不覆盖
- pipe管道符|
- 将左边程序的输出作为右边程序的输入
- ls -l /|tail -n1
- root 用户
- sudo 以管理员身份运行程序（但是不能输出流到无权限的文件文件）
- sudo su变成root用户
- echo 1060 | sudo tee brightness

tee将输入内容写入一个文件，同时输出

find

xdg-open +文件名在适当程序打开文件
