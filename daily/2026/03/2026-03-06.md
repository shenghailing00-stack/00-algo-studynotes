# 2026-03-06 study log
“应该一种类型，先看下题目学下方法，然后把对着标准代码、过脑子地敲一遍，然后再揣摩揣摩怎么想的，然后再问ai，在全网的oj里帮你找相似考点的题目，然后疯狂练习，最后就理解了”——from zhy
- 先又重新过了一下昨天滑动窗口的题目，左闭右开一定注意把边界写死。找Gemini再练几道。
 - 54.螺旋矩阵https://leetcode.cn/problems/spiral-matrix/description/（反过来）
   - **四边界收缩**——终于清楚了终于清楚了！这个方法比之前gemini给的方便多了！
     - 空值处理： 当 matrix 为空时，直接返回空列表 [] 即可。
     - 初始化： 矩阵 左、右、上、下 四个边界 l , r , t , b ，用于打印的结果列表 res 。
     - 循环打印： “从左向右、从上向下、从右向左、从下向上” 四个方向循环打印。
     - 根据边界打印，即将元素按顺序添加至列表 res 尾部。**matrix[t][i]:第top行第i列**
     - 边界向内收缩 1 （代表已被打印）。
     - 判断边界是否相遇（是否打印完毕），若打印完毕则跳出。
     - 返回值： 返回 res 即可。
     - <img width="1249" height="1294" alt="image" src="https://github.com/user-attachments/assets/af65e2ed-2b48-4d2c-aeb1-3ba3916fab7d" />
     ```python
     class Solution:
     def spiralOrder(self, matrix: List[List[int]]) -> List[int]:
        if not matrix: return []
        l, r, t, b, res = 0, len(matrix[0]) - 1, 0, len(matrix) - 1, []
        while True:
            for i in range(l, r + 1): res.append(matrix[t][i]) # left to right
            t += 1
            if t > b: break
            for i in range(t, b + 1): res.append(matrix[i][r]) # top to bottom
            r -= 1
            if l > r: break
            for i in range(r, l - 1, -1): res.append(matrix[b][i]) # right to left
            b -= 1
            if t > b: break
            for i in range(b, t - 1, -1): res.append(matrix[i][l]) # bottom to top
            l += 1
            if l > r: break
        return res
     ```
- 前缀和思路练习：44.开发商购买土地https://kamacoder.com/problempage.php?pid=1044
  - 
